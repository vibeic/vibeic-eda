# vibeic-eda — 48-fix program status

Live scoreboard for the OSS-EDA fork initiative (`OSS_EDA_FORK_ROADMAP.md`). Every
`DONE` row has a reproducible FAIL→PASS proof that the repo-gatekeeper re-ran and
verified before integrating into `vibeic-eda`. `ADOPTED` = closed by adopting the
current upstream tree (roadmap was against an older version); proven working, no
custom patch. `DEFERRED` = algorithm-hard research port, honestly not attempted/
partial. `EXTERNAL` = a fork genuinely cannot manufacture (foundry data).

Legend: ✅ DONE-proven · 🟢 ADOPTED-proven · ◐ PARTIAL(real progress, fork-committed) · 🔷 DEFERRED(algorithm-hard) · ⚪ EXTERNAL

## Tool 1 — OpenROAD  (fork: vibeic/OpenROAD, built native, in vibeic-eda:0.1.0)
| Fix | Class | Status | Proof (gatekeeper-verified) |
|---|---|---|---|
| post-detailed-route repair on real parasitics: Signal-11 crash-fix + `-detailed_routing` flag + kDetailedRouting→Steiner | algorithm-hard | ✅ | sha256 routed/ss/OpenRCX: stock segfaults; patched repair 8 resized+4 buf, max-slew **289→0**, exit 0 |
| repair_design pass-2 segfault (ECO re-run on buffered netlist) | bug-fix-easy | — | **non-reproducible** on current build (double repair_design → PASS2_DONE_NO_CRASH, exit 0); no fix needed on this version |
| in-flow incremental reroute of post-route ECO buffers | algorithm-hard | ✅ | **DONE — closes end-to-end on gcd_nangate45, three fork fixes (`8d040d5`,`1bf7761`,`9f2a14f`), all in 0.2.3.** Reframed (via 3-agent source study) from "missing algorithm" to "assemble the existing incremental machinery + harden it": the sanctioned ECO bracket — `remove_fillers` → `global_route -start_incremental` → `repair_design` → `detailed_placement` → `-end_incremental` → `detailed_route`. **VERIFIED on gcd** (17 buffers in 7 nets): `detailed_route` rc=0, **0 fatal errors, 0 DRC**, unrouted back at the design's 34-net baseline; **all 24 buffered/affected nets routed**; the already-routed nets are preserved **BYTE-IDENTICAL** pre==post (DEF diff of `_165_/_233_/_300_`), a buffered net (`net13`) correctly changes 0→31 lines. Three FAIL→PASS fixes, each regression-clean (std gcd/obstruction/top_level_term stay exit0/0-err) and dual-track-verified: **(1) `8d040d5` DRT-0073** — a 0-AP buffer threw across `FlexPA::updateDirtyInsts`'s OpenMP boundary → `std::terminate` SIGABRT; warn `DRT-627`+continue + `ThreadException` guard (baseline exit134→exit0). **(2) `1bf7761` DRT-0155** — `isValidGuideLayerNum` aborted on a multi-gcell below-min-layer (metal1) guide that `genGuides_split()` already splits per-gcell (proven: the no-GCELLGRID first route accepts the identical guide); removed the conservative rejection. **(3) `9f2a14f` DRT-0218** — `genGuides` validated connectivity for EVERY net and aborted on already-routed nets that INCR *preserves* anyway (the identical guide is re-validated against a populated GCELLGRID+committed shapes); warn `DRT-628`+continue for `hasInitialRouting` nets. Independent source-study agent confirmed the zero-guides-for-a-preserved-net state is a pre-existing first-class case (handled like a guide-less power net, `FlexDR.cpp:1320`). |
| RSZ-0089 wire-rc abort, DPL-0033 check_placement abort, DRT-0305 constant-PG abort → warn+continue | easy/medium | ✅ | reclassify each fatal `logger->error`→`warn` so a wrapping flow recovers instead of aborting (`c9a4588`). Not a bare swap: RSZ unifies both branches on a −inf sentinel (no `bad_optional_access`), DRT falls through building the net non-special (`updateDesign` derefs the return → nullptr would segfault). Re-run FAIL→PASS **in `vibeic-eda:0.2.2`**: DPL baseline `[ERROR DPL-0033]` exit 1 → patched `[WARNING DPL-0033]` + `REACHED_AFTER_CHECK_PLACEMENT` exit 0; happy path still exit 0, no spurious warning |
| post-detailed-route **`repair_timing -setup`** on real SPEF (the setup half of `cf06074139`, previously only proven for `repair_design`/max-slew) | algorithm-hard | ✅ | **Proven + regression-locked.** The same `makeBufferedNet` kDetailedRouting→Steiner dispatch that fixed `repair_design` also carries `repair_timing -setup/-hold` (all go `BufferMove::doMove → rebufferPin → makeBufferedNet`), but it was never proven for setup and NO rsz test read a SPEF. **FAIL→PASS on the real sha256 sign-off corner** (`clean_run_v1427`, routed DEF + max-RC OpenRCX SPEF, clk 25.907 ns): real-RC worst-slack **−8.83 ns → +0.33 ns**, 40/40 violating endpoints repaired (13 buf / 1 resize / 2 pin-swap / 4 clone), exit 0, 0.6 s. **Apples-to-apples negative control on the IDENTICAL script:** stock `26Q1-990-g15af3a5c0` **Signal 11** in `GlobalRouter::getPinGridPositions ← makeBufferedNetGroute ← makeBufferedNet ← BufferMove ← RepairSetup::repairPath`; fork `26Q3-111-g3efb695851` exit 0. ECO result is **legal + realizable**: inside the sanctioned incremental bracket (`global_route -start_incremental → repair_timing -setup → detailed_placement → -end_incremental → detailed_route`) `detailed_placement` legalizes the ECO cells (`check_placement` OK). **Regression test `repair_setup_spef1` (commit `42ca417`)** — gcd-Nangate45 routed+SPEF fixture, fresh-session read (no live GR map = crash precondition), `estimate_parasitics -detailed_routing` + `repair_timing -setup -sequence "buffer"`; self-checking (segfault → no marker → non-zero exit), deterministic (−0.043 → −0.029, 23 buf), and a genuine discriminator (stock 26Q1 Signal 11 on the same fixture). Binary capability already in `vibeic-eda:0.2.17`; the plugin still SKIPS the pass (pnr.tcl MEASURE-ONLY block) — wiring tracked upstream as a plugin issue. |

> **IMAGE-INTEGRATION NOTE (2026-07-16, OpenROAD — for the re-bake actor):** the
> `repair_setup_spef1` regression test is a **test-only** commit (`42ca417`, tip of
> `vibeic/post-route-detailed-routing-repair`); it does **not** change the openroad binary,
> so `vibeic-eda:0.2.17` already carries the proven capability. To include the test in the
> fork CI on the next bake, advance `Dockerfile` `OPENROAD_REF` from `3efb695851` to
> **`42ca417`**. Smoke test after re-bake: `docker exec <img> bash -lc 'cd
> $OPENROAD_SRC/src/rsz/test && openroad -no_init -exit repair_setup_spef1.tcl'` must print
> `POST_SPEF_REPAIR_SETUP no_crash improved` and exit 0. No version bump / re-bake performed here.

## Tool 2 — yosys + abc  (fork: vibeic/yosys @ vibeic/synth-fixes, commit e03eb95)

> **IMAGE-INTEGRATION NOTE (2026-07-16, for the re-bake actor — not done here):** to ship the
> `equiv_make -memory_map` fork fix, bump `Dockerfile` `ARG YOSYS_REF` from `1042b3f55…` to
> **`64286d02b`** (tip of `vibeic/synth-fixes-v0.67` after the fix; dev-branch mirror `3cd5b9dc4`
> on `vibeic/synth-fixes`). Smoke test after re-bake: `docker exec <img> yosys -p "help equiv_make"`
> must list `-memory_map`; and a `$mem_v2` miter with `equiv_make -memory_map` must print
> "Equivalence successfully proven!". No version bump / re-bake performed by the yosys-fork agent.

| Fix | Class | Status | Proof (gatekeeper-verified) |
|---|---|---|---|
| LEC of a memory-bearing netlist: `equiv_make -memory_map` legalizes `$mem`/`$mem_v2` before the miter | feature-add-medium | ✅ | **Fork fix (yosys commit `64286d02b` on `vibeic/synth-fixes-v0.67`; mirror `3cd5b9dc4` on `vibeic/synth-fixes`; built + proven in `vibeic-eda:0.2.17`).** Root cause: `equiv_make`'s memory guard only fires on RTLIL::Memory objects (`has_memories()`); a memory collected into a `$mem_v2` CELL (after `memory_collect`/`opt`) slips through, survives into the `$equiv` miter, and makes `equiv_simple`/`equiv_induct` abort `ERROR: No SAT model available for cell ($mem_v2)` — the sha256 LEC left **1067 equiv points unproven** on exactly this one `$mem_v2` cell. Fix: new `-memory_map` runs `memory_collect; memory_map; opt_clean` on BOTH gold+gate BEFORE building the miter (legalizing after equiv_make leaves the sides structurally misaligned → stays unproven — proven order-sensitive); without the flag equiv_make now emits an actionable warning naming the cell+flag instead of silently building an unprovable miter. Re-run FAIL→PASS **in `vibeic-eda:0.2.17`** (yosys 0.67+ `1042b3f55`+patch): baseline → warn + `No SAT model available for $mem_v2`; `-memory_map` equivalent RAM/ROM vs full-synth netlist → **"Equivalence successfully proven!" 0 unproven**; deliberately-broken read address (`ra^1`) → **8 unproven `$equiv`** → `equiv_status -assert` fails (sound negative control). No-regress: equiv_make-using tests (equiv_assume, dfflibmap_formal, cellmatch, lut2mux, muxcover, sklansky/kogge-stone/han-carlson) all pass. **Plugin adoption (LEC `lec_run` should call `equiv_make -memory_map`) filed as vibeic/vibe-ic#155 — not patched here.** |
| at-speed TDF ATPG (DT1) — 400/400 faults ABORTED, 0% coverage | investigated → **NOT a fork gap** | 🟢 | **Honest finding: yosys `sat` HAS the capability; the floor is a PLUGIN-side performance/timeout problem, not a tool limitation — no fork patch.** Proven in `vibeic-eda:0.2.17`: (a) a 2-flop launch-capture fixture DETECTS a real transition fault via `sat -seq 2` (2-vector `{f1:d0=1} {f2:d0=0}` → observable diff at the capture flop); (b) the plugin's OWN first 7 sha256 fault blocks (`_tdf_batch.ys`) run to valid verdicts — **5 DETECTED (`sat -prove trig 0` → "model found: FAIL!" = the launch/capture 2-pattern) + 2 REDUNDANT** = 5/(7−2) = **100 % test coverage on that sample** — at **~33 s/fault**, each importing **101,025 cells / 461,489 SAT vars**. Root cause: each fault re-flattens the full 3×-instantiated core and rebuilds the entire CNF from scratch; 400 faults × ~33 s ≈ **3.6 h ≫ the 1800 s** driver timeout (`transition_atpg_not_run.json`: "timed out after 1800 seconds") → 0 verdicts land → fail-safe all-ABORT → 0%. Fix is plugin-side (cone-of-influence per fault / fault batching / incremental SAT / raise timeout+reduce sample) — filed as vibeic/vibe-ic#154. True at-speed *timing-graded* grading (DT2/DT3) is correctly an OpenSTA K-longest-path architecture, not a yosys `sat` capability. |
| tri-state fanin preservation (implicit `tribuf -logic` in `synth`) | feature-add-medium | ✅ | `assign io=oe?val:'z`: stock→`assign io=val` (oe dropped); patched→**4× `$_TBUF_ .E(oe)`**; 137/137+99/99 no-regress |
| slang duplicate-module source-located diagnostic + nonzero rc | bug-fix-easy | 🟢 | modern yosys bundles slang: two `module foo`→`error: duplicate definition of 'foo'` +note, RC=1 |
| `$readmemh` ROM init propagation | feature-add-medium | 🟢 | modern tree maps ROM init to correct logic (DE/AD/BE/EF), not always-X |
| unpacked-array ports auto-flatten | feature-add-medium | 🟢 | `read_slang` flattens `logic[7:0] foo[0:3]`→`output[31:0] foo` (legacy `read_verilog -sv` rejects) |
| cross-file `import pkg::*` / StateEnumT | feature-add-medium | 🟢 | `read_slang` builds FSM across files (even reverse order); legacy path syntax-errors |
| D-latch mapping to liberty `latch` cells (`dfflibmap`) | feature-add-medium | ✅ | the real gap was `dfflibmap` only matching liberty `ff` groups (abc's seq model is edge-triggered — the wrong place); added `find_latch()` reading the `latch` group + `t:$_DLATCH_?_` legalize (`e03eb95`). Re-run FAIL→PASS **in `vibeic-eda:0.2.2`**: `1 $_DLATCH_P_` unmapped → `1 latch` liberty cell (`direct match for cell type $_DLATCH_P_`); sequential equivalence PROVEN (`equiv_induct`) + negative control fails; FF-only + 24 latch tests identical |
| carry-chain/prefix-adder structuring · equiv SEC closure · gate-level-ripple lift | algorithm-hard | 🟢✅ | **Capability already IN the shipped yosys/abc — verified reachable, now plugin-wired (v1.3.41); NOT a new docker patch.** (1) **SEC closure:** two sequentially-equivalent designs differing by retiming/pipeline-rebalancing FALSELY fail `equiv_induct` at its shallow default `-seq 4` (0/8 proven) — but PASS at `-seq 8` (8/8) and via abc `dsec`/`dprove`; a latency-7-vs-8 buggy pair stays unproven at any depth (sound, not a rubber-stamp). Captured plugin-side: `lec_post_layout_check.build_yosys_equiv_script` + `eda_lvs yosys_equiv` now auto-escalate `-seq 4→16→64`. (2) **Prefix-adder:** the premise "no recognizer exists" was outdated — Brent-Kung is the DEFAULT `$lcu` map and Kogge-Stone/Han-Carlson/Sklansky ship as `choices/`; 32-bit `a+b` depth 128→72 (KS), CEC-equal to ripple. Captured as `prefix_adder_synth_recipe.py` (CEC-gated). **Former residual now RESOLVED (2026-07-08) — new fork pass `lift_adder` (yosys fork commit `023a117e`, shipped in vibeic-eda:0.2.5):** restructuring an ALREADY-gate-level ripple adder into a parallel-prefix form. `extract_fa` recognises the full-adders but PRESERVES the ripple carry topology; the genuinely-missing piece was a `$fa`-chain → word-level `$add` lift. Built it: `lift_adder` walks a maximal width-1 `$fa` chain wired `X[i]->C[i+1]` and — ONLY for a pure unsigned A+B ripple with a constant-0 LSB carry-in and PRIVATE internal carries — replaces it with one word-level `$add` (conservative CEC-safe bails on subtractor / real-CI / observed-carry / alias / self-loop / n<2), after which the prefix techmap lowers it. **Recipe:** `extract_fa -fa -ha; opt_clean; lift_adder; alumacc; techmap -map +/choices/<topo>.v -map +/techmap.v`. **Proven:** 32-bit gate-level ripple AIG-AND depth **128 → 73** (Kogge-Stone), CEC "Equivalence successfully proven!" vs the original ripple; multi-chain (2 ripples → Lifted 2, CEC proven); soundness battery (subtractor / a+b+cin / RTL-add / non-arith logic all Lifted 0, CEC-equal — never mis-lifts). Captured plugin-side as `prefix_adder_synth_recipe.py --gate-level`. **Step-2.7 adversarial review UPHELD safe** + surfaced one narrow standalone hole (an internal carry also observed by an *unresolved-module* cell was undercounted → possible mis-lift; unreachable via the recipe since `hierarchy` hard-errors first, and CEC-caught anyway) — closed as defense-in-depth in fork commit `ec38cf771` (count every bit of a `!known()` cell as a consumer); re-verified clean ripple still lifts + the undefined-module case now bails. |

## Tool 6 — ngspice  (fork: vibeic/ngspice @ vibeic/batch-honesty, commit cdb4fae)
| Fix | Class | Status | Proof (gatekeeper-verified) |
|---|---|---|---|
| `-b` batch honesty: nonzero rc + per-`.measure` PASS/FAIL marker | feature-add-medium | ✅ | failed `.meas`: stock→`failed!` **RC=0** (silent CI pass); patched→`;;MEAS vbogus FAIL` **RC=1** — re-run by gatekeeper on stock vs patched binary |
| `$&<measvar>` scalar → length-1 vector | bug-fix-easy | ✅ | `$&vpk`: stock `no such variable`/empty → patched `0.999952` |
| control-mode `.param` expansion (`tran`/`meas at=`) | feature-add-medium | ✅ | `tran … tend`: stock `TSTOP invalid` RC=1 → patched runs RC=0 |
| native Monte-Carlo (`mc N [uniform\|lhs] var:lo:hi`) | feature-add-medium | ✅ | stock `mc: no such command` → patched 20-run mean/σ/min/max/yield **+ Latin-Hypercube sampling** (`0b63933`): re-run FAIL→PASS — patched `mc 8 lhs seed=1 rval=0:8` = one-per-bin `[1 1 1 1 1 1 1 1]` (ONEPERBIN=1), baseline parses `lhs`/`rval=0:8` as bogus measures (N=0, no sweep). **`.mc` dot-card DONE** (`cdb4fae`): `inpcom` rewrites a deck-body `.mc N …` into `.control/mc/.endc` → same engine; re-run **in 0.2.2** `.mc 20 vmid:0.5:0.75` → `;;MC vmid N=20 … YIELD=90.0000` (baseline: unrecognized card). **MC-resampling idiom validated PDK-agnostic (vibe-ic #142, 2026-07-15): the `mc` engine correctly resamples agauss on 3 real PDKs; every "identical samples" report is the statistical section being OFF in the typical corner, NOT a tool bug — sky130 `tt` σ=6.8e-16 vs `tt_mm` σ=9.8 mV; sg13g2 typical vs `mm_ok=1` σ=9.5 mV (needs PSP103 OSDI); a commercial 180 nm NDA PDK deterministic σ=1.2e-16 vs its agauss MC sections σ=17.2 mV. No fork change — fix is plugin-side (load the mismatch section). NDA: structure+numbers only, no model content.** |
| DC homotopy (gshunt/rshunt continuation) for floating SC nodes | algorithm-hard | ✅ | added a last-resort gshunt stepping after gmin+source stepping in `CKTop` (`cdb4fae`); never runs when a circuit already converges (0 make-check delta, Sparse path). Re-run FAIL→PASS **in 0.2.2** (OPtran off = the pure-DC path): gmin+source stepping fail → `gshunt stepping completed` → `v(mid)=0 v(a)=1` exit 0. Honest scope: the fork's default OPtran already rescues the common case; this covers the OPtran-off pure-DC solve |
| AMS co-sim bridge | algorithm-hard | 🟢 | **Capability already IN-TREE — verified, NOT a new docker patch.** The "AMS bridge" was a misframing: ngspice already does analog/mixed-signal entirely in-tree via XSPICE event-driven digital (`adc_bridge`/`dac_bridge` + `d_*` code models, all 7 `.cm` auto-loaded) AND Verilog-A behavioral models via `openvaf`→`.osdi` (`pre_osdi`). A genuine analog→digital→analog round trip (sine→adc_bridge→d_inverter→dac_bridge→RC) runs identically on stock + fork. No external-simulator co-sim bridge is needed for these flows. The fork's EXISTING general honesty fixes already cover the AMS domain — proven by 3 stock-vs-fork FAIL→PASS demos: a silent `.meas` failure on a mixed-signal deck (stock RC=0 → fork RC=1 `;;MEAS … FAIL`), the same on an OSDI/Verilog-A deck, and a symbolic-TSTOP `.control` mixed-signal deck (stock `TSTOP invalid` → fork runs). |
| PATH packaging | bug-fix-easy | ✅ | **DONE in the Dockerfile** (belongs there, not ngspice source): the runtime stage symlinks `/foss/tools/ngspice/bin/ngspice → /foss/tools/bin/ngspice`. Verified in `vibeic-eda:0.2.3`: `which ngspice` → `/foss/tools/bin/ngspice`. |
| Build: MAKE_EXIT=0, `make check` 57/58 (1 fail = env graphics, identical on stock → 0 regressions) |||| 

## Tool 4 — magic + netgen  (fork: vibeic/{magic,netgen} @ vibeic/lvs-fidelity)
netgen commits 29852b6+b7d4138 · magic commit 5aea4c2 · both pushed
| Fix | Tool | Class | Status | Proof (gatekeeper-verified) |
|---|---|---|---|---|
| `Final result:` reflects transistor-property errors | netgen | bug-fix-easy | ✅ | **re-run by gatekeeper**: R=1k vs 2k → stock `Circuits match uniquely` (silent LVS false-pass!) → patched `do NOT match uniquely (property errors present)` |
| portless top-cell guard (non-proxy port count) | netgen | bug-fix-easy | ✅ | portless `.subckt`: stock `match uniquely` → patched `do NOT match (no ports to anchor)` |
| `-auto-global` (derive globals from `.global`) | netgen | feature-add-medium | ✅ | `.global` one-side: stock `failed pin matching` → patched `match uniquely` |
| `-nopower="…"` PG-as-global | netgen | feature-add-medium | ✅ | caller-named PG nets: baseline mismatch → patched `match uniquely` |
| black-box leaf positional pin match (+`&&`→`&` typo) | netgen | feature-add-medium | ✅ | defined-vs-stub leaf: stock `failed pin matching` → patched `match`; 3-pin stub still fails (negative control) |
| `ext2spice` label→port promotion (`port makeall` default on) | magic | feature-add-medium | ✅ | scmos NMOS: stock `.subckt top` (empty) → patched `.subckt top DRN SRC Gnd GATE` — feeds netgen portless guard |
| `def/gds read` RECT-null crash | magic | bug-fix-easy | 🟢 | already fixed upstream in 8.3.671 (proven: RECT-before-LAYER reads RC=0, no segfault) |
| **unknown-layer→retain** (`def read`) | magic | bug-fix-easy | ✅ | `DefAddRoutes` dropped an unmapped-layer route entirely (`continue`) + desynced the SPECIALNET parser (width→via-name). Now retains geometry on `TT_TECHDEPBASE` + warns (`3f77653`). Re-run FAIL→PASS: met5 route → baseline **2 errors, geometry gone**; patched **1 warning, shape kept**; known-layer DEF byte-identical (0 regress) |
| NDR/unresolvable-via retain · SPECIALNET power-name propagation | magic | easy/medium | ✅ | completes the DEF-fidelity trio (`5aea4c2`, `lef/defRead.c`). (A) an unresolved via name dropped the cut (silent metal open) → retain the real adjacent-layer tech contact (via up, fallback down) + warn; (B) `SPECIALNET + USE POWER/GROUND` name was lost → propagate onto a label so LVS can anchor. Re-run FAIL→PASS **in 0.2.2**: NDR-via extract node count **2→1** (open→connected) + `retaining a "v" via`; SPECIALNET `.mag` **0→2** VPWR/VGND labels (ext2spice `.subckt` gains `VPWR VGND` ports); resolvable vias + normal DEFs identical |

## Tool 5 — iverilog + Verilator  (fork: vibeic/iverilog @ vibeic/sv-tb-coverage)
iverilog commits e1e12f6+110cadd · pushed · verilator forked, no commit (no honest fix — reported)
| Fix | Tool | Class | Status | Proof (gatekeeper-verified) |
|---|---|---|---|---|
| `->>` nonblocking-event: `-t null` elaborates but vvp codegen segfaults | iverilog | bug-fix-easy | ✅ | **re-run by gatekeeper**: event referenced only by `->>` → stock **SEGFAULT rc=139** → patched compiles rc=0 + `hits=2 PASS` (3 root bugs: nodangle nb-trig, stale `sorry`, schedule_propagate); full suite SV 902/908 + Verilog 1765/1768 **0 regressions** |
| comp-unit package-before-import ordering (driver hoists pkg-declaring files) | iverilog | feature-add-medium | ✅ | use-before-decl: stock `syntax error/Invalid module item` rc=2 → patched `WIDTH=16 PASS`; comment/string-skip negative control holds |
| full-array assignment-pattern `'{...}` | iverilog | feature-add-medium | 🟢 | already upstream in v14.0-devel (proven on stock: decl-init + procedural PASS) |
| `break;`/`continue;` in loops | iverilog | feature-add-medium | 🟢 | already upstream (proven: `sum=4 PASS`) |
| downgrade BLKLOOPINIT/WIDTH/LATCH/COMBDLY | Verilator | bug-fix-easy | ⚪ | **obsolete on v5.051** (honest): each already `-Wno`-suppressible; BLKLOOPINIT's only trigger is also rejected by iverilog → downgrading would emit silently-wrong sims. No honest fix → no patch |
| event-driven `--timing` scheduler · Verilator CDC/NBA region parity | Verilator | — | 🟢 | **ADOPTED-upstream on the shipped 5.048** (proven): `--timing` (`#delay` / named-event / `wait` / fork-join / intra-assignment NBA delay) is byte-identical to iverilog, and NBA read-old-value + cross-clock CDC parity holds — the historical gaps existed only on old 5.020 and are fixed upstream. No honest custom fix warranted; inventing one would be dishonest |

## Tool 3 — klayout  (fork: vibeic/klayout @ vibeic/svrf-native-drc, commit 884e4a1)
> `svrf-native-drc` = the earlier `streamout-fixes` set (below) **plus** the native
> SVRF/Calibre DRC engine + `svrfdrc` buddy (last row), shipped in `vibeic-eda:0.2.11`.

| Fix | Class | Status | Proof (gatekeeper-verified) |
|---|---|---|---|
| foundry layer-map instead of compact 1..N fallback | feature-add-medium | ✅ | **re-run by gatekeeper**: stock `met1.PIN=1/2` (Magic-unreadable, breaks LVS) → patched+map `met1.PIN=68/16`, `via1=67/44`, `met1.NET=68/20` (real sky130 layer/datatypes) |
| honor tech-LEF `MANUFACTURINGGRID`, snap instance + via coords | bug-fix-easy | ✅ | **re-run by gatekeeper**: `OFFGRID_VERTICES 8 → 0` (vs 5nm grid); no-op on grid-legal geometry (default-ON, 90/90 tests pass) |
| merge-abutting streamout (union same-layer polys across instances) | feature-add-medium | ✅ | met1 abutting polys `2 → 1` (opt-in `KLAYOUT_LEFDEF_MERGE_ABUTTING=1`); deferred to `finish()` to avoid dangling-`db::Cell*` SIGSEGV |
| native SVRF/Calibre DRC-deck execution — `svrfdrc` C++ buddy (no commercial license, no Python interpreter) | feature-add-hard | ✅ | **shipped 0.2.11.** `db::parse_deck` + `db::SVRFEngine` (in `src/plugins/tools/svrf_drc/db_plugin/`, baked into the fork's `libklayout_bd.so`) + entry `src/buddies/src/bd/svrfdrc.cc` run a foundry Calibre/SVRF `.rule` deck directly on KLayout's DRC engine. **Byte-identical to the (now-retired) reference Python interpreter on the REAL commercial Key Foundry HP18E80 deck** (`Calibre_HP18E80_DRC_D4.20.rule`, 87,620 lines → 224 layers / 15,897 derivations / 4,533 rules) run on the spm sign-off GDS: tally `{'PASS':4523,'FAIL':10}`, all 4,533 verdict lines match, ~0.9 s. Verified in-image via `command -v svrfdrc` under the real runtime env AND on 4 synthetic corpora (`tests/run_{parse,engine}_parity.sh` vs frozen goldens). Runtime install is a wrapper (prepends the fork lib dir to `LD_LIBRARY_PATH` so the fork's `libklayout_bd.so` — the one carrying the `svrfdrc` symbol + engine — wins over the stock lib). NDA deck verified locally, never committed; all repo fixtures SYNTHETIC. |
| Regression: `dbLEFDEFImportTests` 90/90 pass, identical stock vs patched; each fix's OFF path reproduces stock exactly |||| 

## Tool 7 — analog auto-layout  (Bucket-T; candidate fork: vibeic/ALIGN + vibeic/ALIGN-pdk-sky130)
| Gap (Bucket-T) | Class | Status | Proof / evidence |
|---|---|---|---|
| A5 analog place-and-route: `eda_analog_layout` streams **empty geometry** while reporting success (vibe-ic #144) — no OSS analog auto-P&R was wired | feature-add-hard | ◐ | **SPIKE-PROVEN, image-integration pending. ALIGN produces real GDS+LEF from the run's ACTUAL LDO.** Installed `align-analoglayout` 0.9.8 (cp312 binary wheel, no C++ build) into a scratch `--target` in the running `0.2.17` container (gdspy built from source; COIN-OR soname shim `libCgl.so.1 → auditwheel-mangled`) — **no baked file touched**. Engine `from align import PnR` OK (SeqPair-ILP placer + MILP router, pybind11). **Mock PDK (Bulk65nm):** the LDO topology (bias mirror + NMOS diff-pair + PMOS mirror-load + PMOS pass + Miller + divider, `sky130_fd_pr__*→n/p`) → `LDO_0.gds` **202 KB / LEF 46.54×141.8 µm / 23 layers / 3039 polygons / SHORT:0 OPEN:0** (+shipped `five_transistor_ota` smoke → 1543 polys). **Real sky130 (`ALIGN-pdk-sky130`, official, HEAD 2026-07-05):** same LDO ran through primitive-gen + placement + **routing (SHORT:0 OPEN:2)**, blocked at final GDS on `KeyError 'm1Pitch'` = ALIGN-0.9.8-vs-community-PDK config-schema skew (bounded). Device constraints catalogued for the MCP mapper: W = k·210 nm fin pitch; fins/device < cell height; mirror ratio via `m` not W; MOM cap ≤ 1000 fF (mock). Not DRC-clean (spike bar = any real geometry). Plan: `ANALOG_LAYOUT_ROADMAP.md` — new 0.2.x Docker stage (bake wheel+gdspy+shim+sky130 PDK) → MCP `eda_analog_layout` shells `schematic2layout.py` → GDS/LEF → native `svrfdrc` DRC gate. Effort: mock LOW / sky130-DRC MEDIUM / sg13g2 HIGH. MAGICAL rejected (stale, no clear sky130 DRC path); glayout/OpenFASOC + IHP gdsfactory kept as device-generator substrates. |

## Consolidated image + validation status
- **All 6 tool forks proven + pushed** to the `vibeic` GitHub org, each with a gatekeeper-re-run FAIL→PASS.
- **Full `vibeic-eda:0.2.0` image BUILT + smoke-validated** (all 6 builder stages exit 0): OpenROAD, yosys (CMake+slang), ngspice, magic+netgen, iverilog, klayout. Smoke = **7/7 tools present + runnable** in a container off the image. One build-time fix was needed: the base runs as user 1000 and `/foss/tools` is root-owned, so the stale-ABI cleanup (`rm -rf` of the base tool dirs) aborted with permission-denied — wrapped the runtime-stage mutations in `USER root` → restore `USER 1000`. The base's old-ABI `ghdl.so` yosys plugin is now removed before the fork COPY, so yosys 0.66 no longer symbol-lookup-crashes on synth (verified: `read_verilog → synth → abc` emits a mapped netlist, exit 0).
- **yosys version-jump de-risked:** modern 0.66 synthesizes a real sky130 gate netlist (28× `sky130_fd_sc_hd` cells via `synth → dfflibmap → abc -liberty`) — the 0.4x→0.66 uplift is safe for Phase-2 synth.
- **Fix tally (honest):** ✅ DONE-proven = **30 individual fixes across 23 table rows** (some rows bundle 2–3 sub-fixes: reroute row = 3, severity-reclass row = 3, NDR·SPECIALNET row = 2) — OpenROAD 7 [+RSZ-0089/DPL-0033/DRT-0305 severity reclass, +incremental-reroute trio DRT-0073/0155/0218], yosys 3 [+D-latch→liberty, +`lift_adder` gate-level-ripple→`$add` pass, fork commit `023a117e`, shipped 0.2.5], ngspice 6 [+`.mc` dot-card, +DC gshunt-homotopy], magic+netgen 9 [+NDR-via, +SPECIALNET], iverilog 2, klayout 3 · 🟢 ADOPTED / capability-present-proven = **8 + 2** (yosys 4, iverilog 2, magic 1, Verilator `--timing`+CDC/NBA; **+ abc SEC/prefix-adder and ngspice AMS — capability already in the shipped binary, verified reachable, plugin-wired v1.3.41, NOT new docker binary patches**) · 🔷 DEFERRED algorithm-hard = **0** (the last residual — the gate-level-ripple→`$add` recognizer — was BUILT as `lift_adder`, proven 128→73 depth + CEC-equal, 2026-07-08) · ⚪ EXTERNAL/obsolete = Verilator lint warnings (obsolete on v5.048+; downgrading would emit silently-wrong sims → correctly NO patch). PATH-packaging is DONE in the Dockerfile (ngspice symlinked onto `/foss/tools/bin`, verified in 0.2.3 — a build item, not a fork patch, not counted in the 30). **No inflation:** the abc SEC/prefix-adder + ngspice AMS resolutions still add ZERO to the docker DONE fixes (capability confirmations + plugin-side captures); the +1 (29→30) is the genuinely-new `lift_adder` fork pass, a real FAIL→PASS binary patch shipped in 0.2.5 (added `023a117e`, hardened `ec38cf771` after Step-2.7 review).
- **Container ref flip DONE** (`iic-eda → vibeic-eda`): MCP `index.js` default + `.mcp.json` `EDA_CONTAINER`, the Python-runner `--container` defaults, and `docs/INSTALL.md` + `mcp-eda/INSTALL_GUIDE.md`/`README.md`/`.mcp.json.example` all target the `vibeic-eda` container. A `vibeic-eda` container off `vibeic-eda:0.2.0` is running (fork yosys `c5c8f65d8` confirmed live inside it). Docs now recommend the enhanced image (build from `tools/vibeic-eda/Dockerfile`) with the stock base as a same-named fallback.
- **Two more DEFERRED items closed this round** — verified FAIL→PASS, pushed to the forks, and **built into `vibeic-eda:0.2.1` + re-verified LIVE inside the running container**: ngspice native-MC Latin-Hypercube sampling (`vibeic/ngspice` `0b63933` — in-container `mc 8 lhs` → `sampler=lhs ONEPERBIN=1`) + magic unknown-layer DEF retain (`vibeic/magic` `3f77653` — in-container met5 read → `retaining geometry on generic fallback layer`). The `vibeic-eda` container now runs 0.2.1 (7/7 tools).
- **Eight more fixes closed this round → `vibeic-eda:0.2.2`** (all verified FAIL→PASS, pushed to forks, and **re-verified LIVE inside the running 0.2.2 container**): OpenROAD RSZ-0089/DPL-0033/DRT-0305 severity reclass (`c9a4588`), yosys `$_DLATCH_`→liberty-latch mapping (`e03eb95`), ngspice `.mc` dot-card + DC gshunt-homotopy (`cdb4fae`), magic NDR-via retain + SPECIALNET power-name (`5aea4c2`). Container swapped to 0.2.2 (image built with the 4 new fork SHAs cache-busted; OpenROAD stage rebuilt from source). Verilator `--timing`/CDC-NBA closed as ADOPTED-upstream (proven identical to iverilog on the shipped 5.048).
- **OpenROAD incremental reroute — now ✅ DONE** (fork `9f2a14f`, in 0.2.3): the sanctioned ECO bracket closes end-to-end on gcd_nangate45 — `detailed_route` rc=0, 0 DRC, all 24 buffered nets routed, already-routed nets preserved byte-identical. Three regression-clean fork fixes (`8d040d5` DRT-0073 OMP-crash, `1bf7761` DRT-0155 multi-gcell-metal1 guide, `9f2a14f` DRT-0218 preserve already-routed nets). The DRT-0218 wall — re-validating an already-routed net's guides against a populated GCELLGRID looks disconnected, but INCR preserves that net anyway — was the key close; an independent source-study agent confirmed zero-guides-for-a-preserved-net is a pre-existing supported state. See the Tool-1 row.
- **Both remaining "deferred ports" investigated → resolved as capability-already-present (2026-07-08), plugin-wired v1.3.41:** (1) **abc SEC + prefix-adder** — the capability is already in the shipped yosys/abc; the gap was invocation, not a missing algorithm. SEC: default `equiv_induct -seq 4` falsely fails retiming-equivalent designs → plugin now auto-escalates `-seq 4→16→64` (`lec_post_layout_check.build_yosys_equiv_script` + `eda_lvs yosys_equiv`); independently verified (0/8→8/8, buggy pair stays unproven, abc `dsec` agrees). Prefix-adder: Brent-Kung default + Kogge-Stone/Han-Carlson/Sklansky choices already ship (128→72 depth, CEC-equal) → captured as `prefix_adder_synth_recipe.py`. (2) **ngspice AMS** — already in-tree (XSPICE + openvaf/OSDI), covered by the fork's existing honesty fixes (3 stock-vs-fork FAIL→PASS demos). **The last genuinely-open item — a new arithmetic-recognition pass lifting an already-gate-level ripple back to a word-level adder — is now BUILT (2026-07-08):** `lift_adder` (yosys fork `023a117e`, shipped `vibeic-eda:0.2.5`), proven 32-bit ripple depth 128→73 (Kogge-Stone) with CEC "Equivalence successfully proven!", sound on a subtractor/carry-in/RTL-add/logic battery (never mis-lifts), plugin-wired as `prefix_adder_synth_recipe.py --gate-level`. **No open algorithm-hard residual remains.** Nothing shipped unproven; captures re-verified in the shipped image and their plugin gates unit-tested.
- **Native SVRF/Calibre sign-off DRC — now ✅ DONE → `vibeic-eda:0.2.11`** (klayout fork `vibeic/svrf-native-drc`, commit `884e4a1`). A foundry's Calibre/SVRF `.rule` DRC deck was previously an admitted *commercial-license* gap; it is now executed NATIVELY in C++ inside the klayout fork — parser `db::parse_deck` + engine `db::SVRFEngine` compiled into `libklayout_bd.so`, driven by the `svrfdrc <deck> <layout> <report> [--cell=TOP]` buddy. **Proven byte-identical to the (now-deleted) reference Python interpreter on the REAL commercial Key Foundry HP18E80 deck** (87,620 lines → 224 layers / 4,533 rules) on the spm sign-off GDS: `{'PASS':4523,'FAIL':10}`, all 4,533 verdict lines match, ~0.9 s — verified through the baked buddy in the 0.2.11 image (`command -v svrfdrc` under the real runtime env, exactly as the vibe-ic plugin invokes it) AND on 4 synthetic corpora (`tests/run_{parse,engine}_parity.sh` vs frozen goldens). Runtime install is a wrapper that prepends the fork lib dir to `LD_LIBRARY_PATH` (else the stock `libklayout_bd.so`, which lacks the `svrfdrc` symbol, would win → `undefined symbol`). The Python interpreter (`svrf_klayout/*.py`) is fully retired. NDA deck verified locally, never committed; all repo fixtures SYNTHETIC.
- **EDA tools resolve on a non-login `docker exec` PATH → `vibeic-eda:0.2.12`** (2026-07-12): the stock iic-osic-tools base only puts `/foss/tools/*` on PATH via `/etc/profile.d/iic-osic-tools-setup.sh`, which runs for **login** shells only — so a bare `docker exec vibeic-eda yosys --version` (the exact command in this repo's README Quick Start, and the idiom the Vibe-IC MCP uses via `bash -c`) failed with `executable file not found in $PATH`. Fix (Dockerfile runtime stage, after the `USER 1000` restore): bake the tool dirs into a global `ENV PATH=/headless/.local/bin:/foss/tools/bin:/foss/tools/sak:/foss/tools/kactus2:/foss/tools/klayout:/foss/tools/osic-multitool:${PATH}` — additive (login shells still re-prepend via profile.d, harmless duplicate), so tools resolve WITHOUT a login shell or a per-command `export PATH`. All prior stages byte-identical → CACHED (only the trailing ENV layer is new; no from-source rebuild). Verified LIVE on 0.2.12: bare `docker exec vibeic-eda yosys --version`→`Yosys 0.66+229`, `openroad -version`→`26Q3-111-g3efb695851`, plus sta/iverilog/verilator/magic/netgen/ngspice/klayout all resolve bare; the Vibe-IC MCP `eda_doctor` reports **14/14**. `latest` (GHCR) re-pointed to 0.2.12.
